# 로컬 개발 / 라즈베리파이 클라이언트 전체 환경용 (pytest, 오디오 클라이언트 의존성 포함).
# 운영 서버(EC2)에는 이 파일을 설치하지 마세요 — requirements-backend.txt만 사용합니다.
# (docs/PROJECT_STRUCTURE.md 배포 절차 참고)

# Backend
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.31

pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.2

httpx==0.28.1
defusedxml==0.7.1
openai==2.53.0
slowapi==0.1.10
tenacity==9.1.4

# Dev / Test
pytest>=8.0.0
# tests/test_workflow_definitions.py 가 GitHub Actions 정의를 파싱합니다.
# (CI 에서는 bandit 이 간접적으로 끌어오지만, 간접 의존에 기대지 않습니다.)
PyYAML>=6.0

# Raspberry Pi Client
requests==2.33.0
sounddevice==0.4.6
scipy>=1.14.0,<2.0.0
# RPi.GPIO: 라즈베리파이에서 직접 설치 → pip install RPi.GPIO
